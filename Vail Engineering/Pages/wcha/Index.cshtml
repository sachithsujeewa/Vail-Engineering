@page
@model Vail_Engineering.Pages.wcha.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WasteChapter[0].WasteChapterCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WasteChapter[0].WasteChapterDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.WasteChapter) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WasteChapterCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WasteChapterDescription)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
